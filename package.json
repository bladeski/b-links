{
  "name": "blinks",
  "version": "1.0.0",
  "description": "A curated list of useful links.",
  "author": "Jonathan Blades",
  "license": "MIT",
  "source": "src/index.html",
  "publicUrl": "https://bladeski.github.io/b-links/",
  "scripts": {
    "clean:dist": "rimraf dist",
    "clean:dist-serve": "rimraf dist-serve",
    "start": "npm run clean:dist-serve && parcel serve src/index.html src/robots.txt --dist-dir dist-serve",
    "build": "npm run clean:dist && parcel build src/index.html src/robots.txt --no-source-maps"
  },
  "browserslist": "since 2015",
  "devDependencies": {
    "@parcel/transformer-sass": "2.7.0",
    "parcel": "^2.7.0",
    "ts-loader": "^9.4.1",
    "typescript": "^4.8.4",
    "rimraf": "^3.0.2"
  }
}
